import React, { FC } from 'react';
import SearchUnit from '../../../containers/Search/SearchUnit';

const Search: FC = () => {
  return (
    <main>
      <SearchUnit />
    </main>
  );
};

export default Search;
