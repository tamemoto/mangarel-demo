import React, { FC } from 'react';
import Calendar from 'containers/Home/Calendar';

const Home: FC = () => (
  <main>
    <section>
      <Calendar />
    </section>
  </main>
);

export default Home;
